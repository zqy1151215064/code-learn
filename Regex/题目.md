# 题目一

```javascript
题目要求：将字符串中字符出现多次的替换成')', 只出现一次的替换成'('

<!-- test -->
din => (((
recede  => ()()()
(( @  => ))((


const rep = text => text.replace(/(.)(?=.*\1|(?<=\1.+))|./g,(_,g)=> '()'[+!!g])

```
